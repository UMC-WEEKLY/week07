server:
  port: 9000  # 9000번 포트로 실행

spring:
  application:
    name: demo

  datasource:
    platform: mysql
    url: jdbc:mysql://127.0.0.1:3306/umc_week7?useUnicode=true&serverTimezone=Asia/Seoul
    username: root # 만약 따로 설정하지 않으셨다면 기본값은 admin 입니다
    password: '' # 비밀번호를 입력해주세요!
    driver-class-name: com.mysql.cj.jdbc.Driver
